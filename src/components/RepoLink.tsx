import React from "react";
import { Typography } from "@mui/material";

const RepositoryLink: React.FC = () => {
  return (
    <Typography variant='body1' paragraph>
      Welcome to The Sanatani Group
    </Typography>
  );
};

export default RepositoryLink;
